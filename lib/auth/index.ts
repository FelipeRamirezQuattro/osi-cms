/**
 * Thin auth adapter (see CLAUDE.md, constraint 2). Nothing outside this
 * file should import @supabase/ssr's auth helpers directly — the admin
 * middleware, admin layout, and Server Actions all go through here.
 *
 * Populated in Phase 5 once admin_profiles and roles exist.
 */
export {};
